import React from 'react';
//@ts-expect-error
import App from 'host-remote/Shell';

// const container = document.getElementById('app');
// const root = createRoot(container!);
// root.render(<App />);

export default App;
